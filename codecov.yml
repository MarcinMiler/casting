coverage:
    status:
        project:
            default: off
            web:
                flags: web
            server:
                flags: server

flags:
    server:
        paths:
            - server/src
    web:
        paths:
            - web/src
